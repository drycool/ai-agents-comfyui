# AI Agents System for ComfyUI

**Локальная система AI-агентов для автоматизированной обработки изображений и генерации**

---

## Оглавление

1. [Обзор](#обзор)
2. [Архитектура](#архитектура)
3. [Компоненты](#компоненты)
4. [Установка и запуск](#установка-и-запуск)
5. [Использование](#использование)
6. [AI-автоматизация](#ai-автоматизация)
7. [Пайплайны обработки](#пайплайны-обработки)
8. [Конфигурация](#конфигурация)
9. [Расширение системы](#расширение-системы)
10. [План развития](#план-развития)

---

## Обзор

Система представляет собой локальную автоматизированную систему обработки изображений для Instagram-магазинов с использованием AI-моделей. Все компоненты работают локально, не требуя подключения к внешним API.

### Ключевые возможности

- **AI-автоподбор пресета**: автоматический анализ фото и выбор оптимальных настроек
- **Улучшение качества**: AI-powered upscaling и denoising через ComfyUI
- **Генерация описаний**: AI-создание продающих описаний и тегов для постов
- **Полная автоматизация**: от загрузки фото до готового поста
- **Пакетная обработка**: массовая обработка с приоритезацией
- **Три режима генерации**: простой, улучшенный, полный
- **Оркестрация AI-инструментов**: Ollama, Claude CLI, Gemini CLI

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    AI Pipeline Orchestrator                 │
│              (Ollama: LLaMA 3.2 / Qwen)                    │
└─────────────────────┬───────────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
   ┌─────────┐  ┌──────────┐  ┌───────────┐
   │ Claude  │  │  Gemini   │  │  ComfyUI   │
   │   CLI   │  │   CLI     │  │    API     │
   └─────────┘  └──────────┘  └───────────┘
        │             │             │
        └─────────────┴─────────────┘
                      │
              ┌───────▼───────┐
              │ Instagram     │
              │ Processed     │
              │ + Description │
              └───────────────┘
```

### Поток данных (полный пайплайн)

1. **Вход**: Загрузка фото
2. **AI-анализ**: Ollama анализирует фото, выбирает пресет
3. **Обработка**: Применение пресета (яркость, контраст, температура, кадрирование)
4. **Улучшение**: ComfyUI upscaling + denoising (опционально)
5. **Описание**: Claude/Gemini генерируют описание и теги
6. **Выход**: Готовое фото + описание для поста

---

## Компоненты

### 1. Instagram Processor (`instagram_processor.py`)

Основной процессор для обработки фото.

**Функции:**
- `process_image()` — обработка одного фото
- `batch_process()` — пакетная обработка
- `apply_preset()` — применение пресета
- `center_crop_with_offset()` — кадрирование с вертикальным смещением

**Пресеты:**
- `shop_vintage` — магазин (тёплый)
- `warm_vintage` — тёплый винтаж
- `neutral` — нейтральный
- `minimal` — минималистичный

### 2. Vintage Processor (`vintage_processor.py`)

Винтажные эффекты для фото.

**Функции:**
- `apply_sepia()` — сепия
- `apply_grain()` — зерно
- `apply_vignette()` — виньетка

### 3. ComfyUI API Client (`comfy_client.py`)

Клиент для взаимодействия с ComfyUI через REST API.

**Функции:**
- `queue_prompt()` — постановка workflow в очередь
- `get_history()` — получение истории выполнения
- `execute_workflow()` — выполнение workflow с ожиданием
- `get_images()` — получение сгенерированных изображений

### 4. CLI Wrappers (`cli_wrappers.py`)

Обёртки для CLI инструментов.

#### OllamaWrapper
- `run()` — выполнение промпта через модель
- `list_models()` — список доступных моделей
- `pull()` — загрузка модели
- `check_connection()` — проверка соединения
- `analyze_image()` — анализ изображения

#### ClaudeWrapper
- `run()` — выполнение промпта через Claude
- `enhance_prompt()` — улучшение промпта
- `generate_workflow()` — генерация workflow
- `generate_product_description()` — генерация описания товара

#### GeminiWrapper
- `run()` — выполнение промпта через Gemini
- `suggest_style()` — предложение художественного стиля
- `analyze_image()` — анализ изображения

### 5. Orchestrator Agent (`orchestrator.py`)

Главный агент-оркестратор, координирующий все компоненты.

**Методы:**
- `analyze_request()` — анализ запроса через Ollama
- `get_style_suggestions()` — получение стиля от Gemini
- `enhance_prompt()` — улучшение промпта через Claude
- `generate_workflow()` — создание workflow
- `process_simple()` — простой пайплайн
- `process_enhanced()` — улучшенный пайплайн
- `process_full()` — полный пайплайн с верификацией

### 6. AI Pipeline (`ai_pipeline.py`) — [ПЛАНИРУЕТСЯ]

Полный AI-пайплайн автоматизации.

**Функции:**
- `auto_detect_preset()` — AI-автоподбор пресета
- `enhance_quality()` — улучшение качества через ComfyUI
- `generate_description()` — генерация описания товара
- `full_auto_process()` — полная автоматическая обработка

### 7. Launcher (`launcher.py`)

Удобный интерфейс запуска системы.

**Команды:**
- `--status` / `-s` — проверка статуса всех подключений
- `--list-models` / `-l` — список моделей Ollama
- `--mode` — выбор режима (simple/enhanced/full)
- `--pipeline` — выбор пайплайна (instagram/comfy)

### 8. Web Interface (`app.py`)

Streamlit web-интерфейс для обработки фото.

---

## Установка и запуск

### Требования

- Python 3.10+
- Ollama 0.16.3+ (для AI-анализа)
- Claude CLI 2.1.50+ (для генерации описаний)
- Gemini CLI 0.29.5+ (для анализа изображений)
- ComfyUI с запущенным API (для улучшения качества)

### Шаг 1: Установка зависимостей

```bash
cd D:/newproject
pip install -r requirements.txt
```

### Шаг 2: Настройка ComfyUI (опционально)

Для AI-улучшения качества:

```bash
cd ComfyUI
python main.py --listen 0.0.0.0 --port 8188
```

### Шаг 3: Проверка статуса

```bash
python launcher.py --status
```

### Шаг 4: Запуск web-интерфейса

```bash
streamlit run app.py
```

---

## Использование

### Web-интерфейс

```bash
streamlit run app.py
```

Откроется интерфейс по адресу http://localhost:8501

### Программный API

```python
from instagram_processor import InstagramProcessor

# Простая обработка
processor = InstagramProcessor(output_dir="./output")
result = processor.process_image(
    image_path="photo.jpg",
    preset="shop_vintage",
    jpeg_quality=95
)

print(result)  # {'output': 'path/to/image.jpg', 'size': (1080, 1350), ...}
```

### AI-автоматизация

```python
from ai_pipeline import AIPipeline

pipeline = AIPipeline()

# Полная автоматическая обработка
result = pipeline.full_auto_process(
    image_path="photo.jpg",
    category="vintage_clothing"
)

print(result["image"])      # Обработанное фото
print(result["description"]) # Описание для поста
print(result["hashtags"])    # Хештеги
```

---

## AI-автоматизация

### 1. AI-автоподбор пресета

Автоматический анализ фото и выбор оптимального пресета через Ollama.

```python
from ai_pipeline import AIPipeline

pipeline = AIPipeline()
preset = pipeline.auto_detect_preset("photo.jpg")
# {'preset': 'shop_vintage', 'adjustments': {'brightness': 20, 'contrast': 1.15, 'temperature': 6000}}
```

### 2. AI-улучшение качества

Автоматическое улучшение качества через ComfyUI (RealESRGAN, denoising).

```python
# Улучшение качества фото
enhanced = pipeline.enhance_quality("photo.jpg", scale=2)
```

### 3. AI-генерация описания

Генерация продающего описания для Instagram.

```python
# Генерация описания товара
description = pipeline.generate_description(
    image_path="photo.jpg",
    category="vintage_clothing"
)

print(description["title"])        # "Винтажное платье 70-х"
print(description["description"]) # "Прекрасное платье..."
print(description["hashtags"])     # "#винтаж #мода #..."
```

---

## Пайплайны обработки

### Instagram Pipeline (Основной)

```
Загрузка → AI-анализ → Пресет → Кадрирование → Сохранение
```

### AI Pipeline (Полный)

```
Загрузка → AI-анализ (Ollama) → Пресет → Улучшение (ComfyUI) → Описание (Claude/Gemini) → Готово
```

### Пакетная обработка

```python
from instagram_processor import InstagramProcessor

processor = InstagramProcessor()

# Пакетная обработка
results = processor.batch_process(
    input_dir="./input",
    output_dir="./output",
    preset="shop_vintage"
)
```

---

## Конфигурация

### Файл `.env`

```env
# ComfyUI Settings
COMFYUI_HOST=http://localhost:8188
COMFYUI_LISTEN=0.0.0.0
COMFYUI_PORT=8188

# Ollama Settings
OLLAMA_HOST=http://localhost:11434
OLLAMA_DEFAULT_MODEL=llama3.3
OLLAMA_ORCHESTRATOR_MODEL=llama3.3
OLLAMA_LIGHT_MODEL=phi4

# Claude CLI Settings
CLAUDE_CLI_PATH=claude

# Gemini CLI Settings
GEMINI_CLI_PATH=gemini

# Paths
OUTPUT_DIR=./output
WORKFLOWS_DIR=./workflows
INPUT_DIR=./input

# Instagram Settings
INSTAGRAM_WIDTH=1080
INSTAGRAM_HEIGHT=1350
DEFAULT_QUALITY=95
```

---

## Расширение системы

### Добавление нового пресета

```python
from instagram_processor import InstagramProcessor

processor = InstagramProcessor()

# Добавить новый пресет
processor.add_preset("my_preset", {
    "brightness": 10,
    "contrast": 1.1,
    "temperature": 6000
})

# Использовать
processor.process_image("photo.jpg", preset="my_preset")
```

### Добавление нового AI-агента

1. Создать класс-обёртку в `cli_wrappers.py`:
```python
class NewAgentWrapper:
    @staticmethod
    def run(prompt: str) -> str:
        # Реализация
        pass
```

2. Добавить в `AIPipeline.__init__()`:
```python
self.new_agent = NewAgentWrapper()
```

---

## Roadmap (План реализации)

### Приоритеты выполнения

| # | Задача | Приоритет | Статус |
|---|--------|-----------|--------|
| 1 | AI-автоподбор пресета (Ollama) | 🔴 Высокий | ✅ **Реализовано** |
| 2 | AI-улучшение качества через ComfyUI | 🔴 Высокий | 🔄 Готово к реализации |
| 3 | Генерация описаний товаров (Claude/Gemini) | 🟡 Средний | 🔄 Готово к реализации |
| 4 | Полный автоматический пайплайн | 🔴 Высокий | 🔄 Готово к реализации |
| 5 | Пакетная обработка с приоритезацией | 🟢 Низкий | ⏳ В очереди |
| 6 | Web-интерфейс — AI-вкладка | 🟢 Низкий | ⏳ В очереди |
| 7 | Генерация метаданных и именование | 🟢 Низкий | ⏳ В очереди |

### Описание задач

#### T-001: AI-автоподбор пресета (Ollama)
**Автоматизация: 70%**

- Анализ фото через Ollama
- Автоматическое определение: освещение, цветовой баланс, качество
- Выбор оптимального пресета: shop_vintage, warm_vintage, neutral, minimal
- Рекомендация параметров: яркость, контраст, температура

**Вход:** путь к фото
**Выход:** {preset, brightness, contrast, temperature}

#### T-002: AI-улучшение качества через ComfyUI
**Автоматизация: 85%**

- AI Upscale (RealESRGAN) — увеличение разрешения
- Denoise — удаление шумов
- Color correction — коррекция цвета
- Винтажные эффекты через vintage_processor

**Вход:** путь к фото
**Выход:** улучшенное фото высокого качества

#### T-003: Генерация описаний товаров (Claude/Gemini)
**Автоматизация: 90%**

- Анализ фото через Gemini
- Генерация продающего описания через Claude
- Автогенерация хештегов
- Определение размера и состояния товара

**Вход:** путь к фото
**Выход:** {title, description, hashtags, size, condition}

#### T-004: Полный автоматический пайплайн
**Автоматизация: 95%**

```
Загрузка → AI-анализ → автопресет → улучшение качества → описание → сохранение
```

Объединяет все модули в единый процесс.

#### T-005: Пакетная обработка с приоритезацией

- AI-определение приоритета обработки
- Интеллектуальная сортировка очереди
- Progress tracking
- Отчётность

#### T-006: Web-интерфейс — AI-вкладка

- Вкладка "AI Авто" в app.py
- Показать AI-анализ фото
- Предпросмотр с AI-настройками
- Генерация описания в интерфейсе

#### T-007: Генерация метаданных и именование

- Автоматическое именование файлов по дате/категории
- Сохранение metadata (exif)
- Логирование всех обработок

---

## Версии окружения

| Компонент | Версия |
|-----------|--------|
| Python | 3.10+ |
| Ollama | 0.16.3 |
| Claude CLI | 2.1.50 |
| Gemini CLI | 0.29.5 |
| ComfyUI | latest |

### Доступные модели Ollama

- `llama3.2:latest` (2.0 GB)
- `llama3.3:latest` (4.9 GB)
- `qwen2.5-coder:7b` (4.7 GB)

---

## Структура проекта

```
D:/newproject/
├── .env                      # Конфигурация
├── requirements.txt          # Python зависимости
├── README.md                 # Документация
├── CLAUDE.md                 # Инструкции для Claude
│
├── app.py                    # Streamlit web-интерфейс
├── launcher.py               # Командный интерфейс
│
├── instagram_processor.py    # Обработка фото для Instagram
├── vintage_processor.py     # Винтажные эффекты
├── ai_pipeline.py            # AI-пайплайн [ПЛАНИРУЕТСЯ]
│
├── comfy_client.py           # ComfyUI API клиент
├── cli_wrappers.py           # CLI обёртки (Ollama, Claude, Gemini)
├── orchestrator.py           # Главный оркестратор
│
├── workflows/                # ComfyUI workflows
│   └── sdxl_basic.json
│
├── output/                   # Обработанные изображения
└── input/                    # Исходные изображения
```

---

## Лицензия

MIT License

---

## Авторы

Система создана для автоматизации обработки изображений для Instagram-магазинов с использованием локальных AI-моделей.
